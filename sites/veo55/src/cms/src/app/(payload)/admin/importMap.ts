/* Payload auto-генерирует этот файл при первом запуске админки и старте dev.
 * Пустой на старте — наполняется когда добавляются кастомные admin-компоненты.
 */
export const importMap: Record<string, unknown> = {};
