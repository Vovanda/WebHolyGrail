import type { Block } from 'payload';

/**
 * LitterCard — карточка помёта. Ссылка на запись Litters + опция показа sold-щенков.
 *
 * @remarks
 * Сам блок намеренно простой: один relation на Litters и один флаг. Вся гибкость
 * (флаги показа регалий, визитка пары, описание, статус) живёт в самой записи
 * Litters и редактируется там.
 *
 * **Произвольные секции страницы помёта** (рамка «отборное поведение», врезка,
 * любая нестандартная штука) — ставятся **рядом** с LitterCard на той же
 * странице как обычные Prose/Quote/Hero-блоки. Это и есть «секция для
 * произвольной вставки» из ТЗ: композиция блоков страницы, не поле внутри
 * блока.
 */
export const LitterCardBlock: Block = {
  slug: 'litter-card',
  labels: { singular: 'Карточка помёта', plural: 'Карточки помётов' },
  fields: [
    {
      name: 'litter',
      label: 'Помёт',
      type: 'relationship',
      relationTo: 'litters',
      required: true,
      admin: {
        description: 'Из списка помётов. Вся настройка карточки — внутри самой записи Помёта.',
      },
    },
    {
      name: 'showSold',
      label: 'Показывать проданных щенков',
      type: 'checkbox',
      defaultValue: false,
      admin: {
        description:
          'По умолчанию проданные щенки скрыты с витрины активного помёта. Включить, например, для архивной страницы.',
      },
    },
  ],
};
