import type { CollectionConfig, Endpoint, GlobalConfig } from 'payload';

import { videoDemoCodeEndpoint } from './endpoints/domain/whg/demo-code';

/**
 * Точка сборки CMS. Принадлежит сайту.
 *
 * @remarks
 * Сюда сайт кладёт своё: коллекции предметной области, ручки, задания, глобалы.
 * Конфиг подхватывает этот файл и дописывает названное следом за набором
 * движка - сам конфиг остаётся общим и приезжает обновлением целиком.
 *
 * Прежде своё дописывалось прямо в конфиг, а тот из-за этого не мог ездить.
 * Конфиг замирал на дате рождения сайта: у одного не оказалось ручек движка,
 * у другого - заведения переключателей при запуске, и увидеть это можно было
 * только чтением файла.
 *
 * Настройки сюда не кладутся: имя базы, подпись админки и разрешённые адреса
 * приходят из окружения. Здесь только код.
 */
export const site: {
  collections: CollectionConfig[];
  globals: GlobalConfig[];
  endpoints: Endpoint[];
  // Задание описывается конфигом Payload, тип которого зависит от набора задач
  // сайта; проверять его здесь нечего - список просто передаётся дальше.
  // eslint-disable-next-line @typescript-eslint/no-explicit-any -- тип задания задан набором сайта
  tasks: any[];
} = {
  collections: [],
  globals: [],
  /*
    Витринная ручка: выдаёт код доступа к показательной записи. Другому сайту
    она не нужна - живёт в domain и подключается здесь.
  */
  endpoints: [videoDemoCodeEndpoint],
  tasks: [],
};
