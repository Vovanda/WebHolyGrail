import type { Block } from 'payload';

/**
 * Своя разметка — блок для случаев, которые не покрыл ни один готовый блок.
 *
 * @remarks
 * Нужен как выход из положения: собрать нестандартную секцию, вставить виджет
 * сервиса, быстро проверить идею — не дожидаясь, пока под неё сделают
 * отдельный блок. В конструкторах такой блок есть всегда, и как только его
 * нет, вёрстка начинает протискиваться через текстовые поля других блоков.
 *
 * **Разметка вставляется как есть.** Поэтому блок доступен только тем, кто и
 * так может менять сайт целиком, а поля описаны так, чтобы редактор понимал:
 * ошибка здесь ломает страницу, и чинить её придётся тоже здесь.
 *
 * **Если секция прижилась — её место в отдельном блоке.** Разметка внутри
 * записи не переиспользуется, не проверяется типами и не поедет в другой
 * инстанс через синк. Этот блок — черновик, а не постоянное жильё.
 */
export const CustomMarkupBlock: Block = {
  slug: 'custom-markup',
  labels: { singular: 'Своя разметка', plural: 'Свои разметки' },
  fields: [
    {
      name: 'html',
      label: 'HTML',
      type: 'code',
      required: true,
      admin: {
        language: 'html',
        description:
          'Вставляется на страницу как есть. Незакрытый тег сломает вёрстку ниже по странице — проверяйте перед публикацией.',
      },
    },
    {
      name: 'css',
      label: 'CSS',
      type: 'code',
      admin: {
        language: 'css',
        description:
          'Стили только для этой секции. Пишите селекторы от своего корневого класса, иначе правило заденет остальной сайт.',
      },
    },
    {
      name: 'fullWidth',
      label: 'Во всю ширину',
      type: 'checkbox',
      defaultValue: false,
      admin: { description: 'Без отступов по краям — для секций с фоном или медиа.' },
    },
  ],
};
