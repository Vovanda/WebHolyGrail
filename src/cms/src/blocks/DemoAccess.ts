import type { Block } from 'payload';

/**
 * DemoAccess — «получите код и попробуйте сами».
 *
 * @remarks
 * Блок витрины: показывает, как работает доступ по коду, вместо того чтобы
 * о нём рассказывать. Посетитель берёт код, вводит его и видит, как закрытые
 * видео открываются.
 *
 * Сам генератор кодов включается флагом окружения на стороне сервера и по
 * умолчанию молчит. Поэтому вставить блок в обычный сайт безопасно: кнопка
 * там просто не сработает, а не начнёт раздавать ключи от платного.
 */
export const DemoAccessBlock: Block = {
  slug: 'demoAccess',
  labels: { singular: 'Проба доступа по коду', plural: 'Пробы доступа по коду' },
  fields: [
    {
      name: 'heading',
      label: 'Заголовок',
      type: 'text',
      admin: { description: 'Пусто — «Попробуйте сами».' },
    },
    {
      name: 'text',
      label: 'Пояснение',
      type: 'textarea',
      admin: { description: 'Одна-две фразы о том, что сейчас произойдёт.' },
    },
  ],
};
