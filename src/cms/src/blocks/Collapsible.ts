import type { Block } from 'payload';

/**
 * Складной кусок: заголовок виден всегда, содержимое разворачивается.
 *
 * @remarks
 * Ставится и на страницу, и внутрь статьи. Нужен там, где материал длиннее
 * внимания читателя: отступление, разбор частного случая, подробности для тех,
 * кому они нужны.
 *
 * Содержимое лежит в разметке с самого начала - его видит поисковик и находит
 * поиск по странице. Скрыт только показ.
 */
export const CollapsibleBlock: Block = {
  slug: 'collapsible',
  labels: { singular: 'Складной кусок', plural: 'Складные куски' },
  fields: [
    {
      name: 'title',
      label: 'Заголовок',
      type: 'text',
      required: true,
      admin: {
        description:
          'Виден всегда, по нему разворачивают. Пишите так, чтобы было ясно, что внутри.',
      },
    },
    {
      name: 'body',
      label: 'Содержимое',
      type: 'richText',
      required: true,
      admin: { description: 'Появляется по нажатию. Обычный текст со всем оформлением.' },
    },
    {
      name: 'openByDefault',
      label: 'Показывать раскрытым',
      type: 'checkbox',
      defaultValue: false,
      admin: {
        description:
          'Кусок открыт сразу. Уместно, когда он важнее соседних, а свернули его ради длины.',
      },
    },
  ],
};
