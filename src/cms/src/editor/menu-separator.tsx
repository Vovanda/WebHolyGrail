'use client';

/**
 * Черта между штатными пунктами «+» и блоками сайта.
 *
 * @remarks
 * Сверху то, что есть в любом редакторе, - файл, линия, ссылка на запись;
 * ниже блоки, собранные для этого сайта. Черта показывает, где кончается одно
 * и начинается другое. Нажимать на неё нечего, поэтому она не кнопка.
 */
export function MenuSeparator() {
  return <div role="separator" className="whg-menu-separator" />;
}
