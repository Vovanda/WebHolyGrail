/**
 * Показ картинок из медиатеки: кубик, лента страницы и помощник показа.
 *
 * @remarks
 * Кубик рисуется на сервере и ничего не знает про браузер. Лента и помощник -
 * клиентские и стоят по одному разу на весь сайт: лента открывает кадр на весь
 * экран, помощник наводит резкость, когда кадр пришёл.
 *
 * Счётная часть - выбор вариантов, отбор по скорости сети, место для кнопок -
 * живёт в `lib/media.ts`, `lib/media-network.ts` и `lib/lane.ts`: она не знает
 * разметки и проверяется без запуска сайта.
 */
export { MediaImage } from './MediaImage';
export { MediaReveal } from './MediaReveal';
export { PageLightbox } from './PageLightbox';
