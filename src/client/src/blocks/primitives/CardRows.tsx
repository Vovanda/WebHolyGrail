/**
 * @deprecated Плитка переехала в слой раскладок: `@/blocks/arrangements/CardRows`.
 *
 * @remarks
 * Раскладка делит площадь, а не рисует содержимое, - место ей не среди кубиков
 * вроде кнопок и значков. Прежний путь остаётся ради сайтов, собранных на этом
 * шаблоне: их доменный код зовёт отсюда, и убрать файл сразу значит сломать им
 * сборку (R10). Уйдёт после того, как они получат синк и переедут.
 */
export * from '@/blocks/arrangements/CardRows';
