/**
 * Заголовок, в котором приходит адрес открытой страницы.
 *
 * @remarks
 * Живёт отдельным файлом, а не рядом с middleware: тот объявляет ещё и список
 * путей, на которых срабатывает, и всё, что его импортирует, тянет объявление
 * за собой.
 *
 * Заголовок внутренний - он добавляется к запросу, а не к ответу браузеру.
 */
export const PATHNAME_HEADER = 'x-pathname';

/**
 * Заголовок с тем, что в адресе после вопросительного знака.
 *
 * @remarks
 * Серверные части страницы получают это из своих пропсов, а вот содержимое
 * панели собирает раскладка, и до неё запрос доходит без разбора. Между тем
 * плейлист на странице отдельного видео приходит именно оттуда.
 */
export const SEARCH_HEADER = 'x-search';
