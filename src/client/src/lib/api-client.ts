/**
 * Точка сборки обращений к CMS. Принадлежит сайту.
 *
 * @remarks
 * Сперва всё из движка, ниже свои доменные запросы:
 *
 * ```ts
 * export * from './api-client.engine';
 *
 * export async function getDogBySlug(slug: string) { … }
 * ```
 *
 * Обновление этот файл не трогает, поэтому доменный запрос живёт вечно, а новый
 * общий доезжает сам вместе с движком. Прежде здесь лежал один файл на всё, и
 * синк уносил дописанное сайтом - разом с работающими страницами.
 */
export * from './api-client.engine';
