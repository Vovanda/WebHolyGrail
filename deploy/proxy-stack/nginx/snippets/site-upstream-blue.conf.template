# Blue color upstream (active when /etc/nginx/snippets/<SITE_SLUG>-upstream-active.conf → this)
upstream <SITE_SLUG>_cms { server 127.0.0.1:3001; keepalive 32; }
upstream <SITE_SLUG>_client { server 127.0.0.1:3000; keepalive 32; }
