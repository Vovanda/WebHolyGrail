# Blue color upstream (active when /etc/nginx/snippets/veo55-upstream-active.conf → this)
upstream veo55_cms { server 127.0.0.1:3001; keepalive 32; }
upstream veo55_client { server 127.0.0.1:3000; keepalive 32; }
